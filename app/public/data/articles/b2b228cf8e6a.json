{"id":"b2b228cf8e6a","content":"Debounce async validation correctly with Angular 22 Signal Forms\nA username field looks harmless until you open the Network tab.‎\nThe user types:‎\nsatvatiyasinYour application sends:‎\n/api/users/check?username=s\n/api/users/check?username=sa\n/api/users/check?username=sat\n/api/users/check?username=satv\n...Twelve characters.‎ Twelve validation requests.‎\nYou can solve this with RxJS, custom async validators, subscription cleanup, and cancellation logic.‎ But Angular 22 Signal Forms now has a more focused solution:‎ debounce the HTTP validator itself.\nSignal Forms became stable in Angular 22 and includes first-class APIs for HTTP validation, pending state, request cancellation, and debouncing.‎\nThe Request-Storm Version\nStart with a signal-based registration model:‎\nimport { Component, signal } from '@angular/core';\nimport {\nform,\nFormField,\nminLength,\nrequired,\nvalidateHttp,\n} from '@angular/forms/signals';\ninterface RegistrationModel {\nusername: string;\n}\n@Component({\nselector: 'app-registration',\nimports: [FormField],\ntemplateUrl: './registration.component.html',\n})\nexport class RegistrationComponent {\nreadonly model = signal<RegistrationModel>({\nusername: '',\n});\nreadonly registrationForm = form(this.model, (path) => {\nrequired(path.username, {\nmessage: 'Username is required',\n});\nminLength(path.username, 3, {\nmessage: 'Use at least three characters',\n});\nvalidateHttp(path.username, {\nrequest: ({ value }) => {\nconst username = value();\nreturn username\n? `/api/users/check?username=${encodeURIComponent(username)}`\n: undefined;\n},\nonSuccess: (response: { available: boolean }) =>\nresponse.available\n? null\n: {\nkind: 'usernameTaken',\nmessage: 'Username is already taken',\n},\nonError: () => ({\nkind: 'serverError',\nmessage: 'Could not verify username',\n}),\n});\n});\n}The template can read validation state directly:‎\n<label for=\"username\">Username</label>\n<input\nid=\"username\"\nautocomplete=\"username\"\n[formField]=\"registrationForm.username\"\n/>\n@if (registrationForm.username().pending()) {\n<span>Checking availability...</span>\n}\n@if (\nregistrationForm.username().touched() &&\nregistrationForm.username().invalid()\n) {\n@for (\nerror of registrationForm.username().errors();\ntrack error.kind\n) {\n<span class=\"error\">{{ error.message }}</span>\n}\n}This already handles more than a basic HTTP call.‎\nAngular runs the synchronous rules first, exposes the async state through pending(), and cancels an outstanding validation request when the field value changes.‎\nBut cancellation does not prevent the requests from starting.‎\nType quickly enough and the browser may still initiate several calls before cancelling the older ones.‎\nDebounce Only the Expensive Validator\nAdd one property:‎\nvalidateHttp(path.username, {\ndebounce: 300,\nrequest: ({ value }) => {\nconst username = value();\nreturn username\n? `/api/users/check?username=${encodeURIComponent(username)}`\n: undefined;\n},\nonSuccess: (response: { available: boolean }) =>\nresponse.available\n? null\n: {\nkind: 'usernameTaken',\nmessage: 'Username is already taken',\n},\nonError: () => ({\nkind: 'serverError',\nmessage: 'Could not verify username',\n}),\n});Now the HTTP validator waits until the value has remained unchanged for 300 milliseconds.‎\nThe important detail is where the debounce is applied.‎\nAngular also provides this form-level rule:‎\ndebounce(path.username, 300);That delays the field’s model update itself.‎\nIt means synchronous validation, computed state, submission logic, and every other consumer of the model also waits.‎\nThat is useful for search filters or expensive derived calculations, but it is usually not what we want for username validation.‎\nWith validator-level debounce:‎\nvalidateHttp(path.username, {\ndebounce: 300,\n// ...\n});the model still updates immediately.‎\nThese rules still run on every keystroke:‎\nrequired(path.username);\nminLength(path.username, 3);Only the expensive HTTP request waits.‎\nAngular explicitly separates these two debounce layers for this reason.‎\nVerify It on Your Current Project\nOpen the application and then open:‎\nDevTools → Network → Fetch/XHRBefore adding debounce, type this without pausing:‎\nsignalformsDepending on your typing speed, you should see several requests begin:‎\ncheck?username=sig\ncheck?username=sign\ncheck?username=signa\ncheck?username=signal\n...Now add:‎\ndebounce: 300Reload the page, clear the Network panel, and type the same value again.‎\nYou should see only the final request:‎\ncheck?username=signalformsAngular’s own documentation uses the same observable comparison:‎ typing a twelve-character value in one burst can result in one validation request instead of twelve when debounce is applied.‎\nYou can also temporarily count requests with an interceptor:‎\nimport { HttpInterceptorFn } from '@angular/common/http';\nlet validationRequests = 0;\nexport const validationCounterInterceptor: HttpInterceptorFn =\n(request, next) => {\nif (request.url.includes('/api/users/check')) {\nvalidationRequests++;\nconsole.log(\n'Username validation requests:',\nvalidationRequests,\n);\n}\nreturn next(request);\n};Register it:‎\nimport { provideHttpClient, withInterceptors } from '@angular/common/http';\nbootstrapApplication(AppComponent, {\nproviders: [\nprovideHttpClient(\nwithInterceptors([\nvalidationCounterInterceptor,\n]),\n),\n],\n});This gives you a reproducible before-and-after test on your existing system rather than relying on theory.‎\nDo Not Forget the Pending State\nDuring async validation, a field is neither valid nor invalid:‎\nfield().pending() === true\nfield().valid() === false\nfield().invalid() === false\nfield().errors().length === 0That means this button is incomplete:‎\n<button [disabled]=\"registrationForm().invalid()\">\nCreate account\n</button>It may remain enabled while the availability request is still running.‎\nAccount for both states:‎\n<button\ntype=\"submit\"\n[disabled]=\"\nregistrationForm().invalid() ||\nregistrationForm().pending()\n\"\n>\n@if (registrationForm().pending()) {\nValidating...\n} @else {\nCreate account\n}\n</button>Angular’s submit() API can wait for pending validation, but the UI should still communicate that the form is busy.‎\nThe Trade-Off\nDebouncing reduces server traffic, but it also delays feedback.‎\nFor most username, email, coupon, and tax-ID checks, somewhere around 250–400 milliseconds is a reasonable starting point.‎\nDo not use async validation for rules the browser can check immediately.‎ Run required, email, pattern, and length validation first, then contact the server only when the local value is structurally valid.‎\nEnding\nAsync validation is not difficult because of the HTTP request.‎ It is difficult because of timing, cancellation, stale responses, pending UI, and unnecessary server traffic.‎\nAngular 22 Signal Forms handles most of that lifecycle for us.‎\nThe key is choosing the correct debounce level:‎\ndebounce(path.username, 300);when the entire field should wait, and:‎\nvalidateHttp(path.username, {\ndebounce: 300,\n});when only the expensive server validation should wait.‎\nOne small property can turn twelve requests into one — without hiding immediate validation feedback from the user.‎\nMedium tags:‎ Angular, TypeScript, Web Performance, Frontend Development, Software Architecture"}