{"id":"7c3e2b41424c","content":"9 min read\n8 hours ago\n\n--\n\nEvery large Angular app has one. It starts innocently: a few shared pipes, a button component, a utility function. Then someone adds a service because “everyone needs it.” Then a model, because “it’s used in two places.” Then another service. Then a component that imports a service that imports another service, and now your SharedModule has a circular dependency with CartModule That nobody can explain.\nSharedModule\n├── ProductService ← Used by Catalog AND Cart AND Checkout\n├── Product (interface) ← Used everywhere, but means different things\n├── UserService ← Used by Profile AND Auth AND Orders\n├── FormatPricePipe ← Simple utility\n├── ButtonComponent ← Fine to be here\n└── CartHelper ← Wait, why is Cart logic in Shared?The real problem isn’t the SharedModule itself — it's that we conflated two different things:\n\n• Genuinely shared UI primitives (buttons, inputs, pipes)\n\n• Domain concepts that happen to be referenced by multiple parts of the app\nBounded Contexts solve the second problem. They give you a principled way to decide what belongs where.\n\nWhat Is a Bounded Context?\nA Bounded Context is a boundary within which a particular domain model applies. Inside that boundary, every term has a precise meaning. Outside it, the same word might mean something different.\nIn ShopFlow, the word “Product” appears in at least three contexts:\nContextWhat “Product” meansKey fieldsCatalogSomething a customer browses and considersName, description, images, reviews, variants, price rangeCartA reference to what the customer intends to buyProductId, selected variant, quantity, unit price at time of addingOrdersA historical snapshot of what was purchasedName at time of purchase, price paid, SKU\nIf you use a single Product interface everywhere, it has to carry all these fields at once. It becomes a 25-property God interface that's used differently in each context, making it impossible to evolve any one context without affecting all the others.\nA Bounded Context says: it’s okay for the same word to mean different things in different parts of the app — as long as each context’s definition is internally consistent and the boundaries between contexts are explicit.\n\nShopFlow’s Bounded Context Map\nHere’s the full context map for ShopFlow. Each box is a separate Angular module (or standalone module group) with its own domain model:\n┌─────────────────────────────────────────────────────────────────┐\n│ ShopFlow Application │\n│ │\n│ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ │\n│ │ CATALOG │ │ CART │ │ CHECKOUT │ │\n│ │ │────▶│ │────▶│ │ │\n│ │ - Product │ │ - Cart │ │ - Order │ │\n│ │ - Category │ │ - CartItem │ │ - Address │ │\n│ │ - Review │ │ - Coupon │ │ - Payment │ │\n│ └──────────────┘ └──────────────┘ └──────────────┘ │\n│ │ │\n│ ┌──────────────┐ ┌──────────────┐ │ │\n│ │ ORDERS │ │ PROFILE │ │ │\n│ │ │◀────│ │ │ │\n│ │ - Order │ │ - Customer │ ▼ │\n│ │ - LineItem │ │ - Address │ ┌──────────────┐ │\n│ │ - Shipment │ │ - Preference │ │ SHARED │ │\n│ └──────────────┘ └──────────────┘ │ KERNEL │ │\n│ │ │ │\n│ ┌──────────────┐ │ - Money │ │\n│ │ INVENTORY │ │ - ProductId │ │\n│ │ (admin) │ │ - CustomerId │ │\n│ │ - Stock │ │ - Address │ │\n│ │ - Variant │ └──────────────┘ │\n│ └──────────────┘ │\n└─────────────────────────────────────────────────────────────────┘The Shared Kernel (bottom right) is the only truly shared domain code. It contains concepts that have the exact same meaning across all contexts: Money, ProductId, CustomerId, Address. This is a carefully curated, minimal set — not a dumping ground.\n\nContext-Specific Models: The Same Word, Different Classes\nLet’s make the “different Product in each context” idea concrete:\nCatalog Context — CatalogProduct\ntypescript\n// catalog/domain/catalog-product.entity.ts\nimport { ProductId, Money } from '@shopflow/shared-kernel';export interface ProductVariant {\nvariantId: string;\ncolor: string;\nsize: string;\nprice: Money;\nstockCount: number;\n}export interface ProductReviewSummary {\naverageRating: number;\ntotalReviews: number;\n}export class CatalogProduct {\nconstructor(\nreadonly id: ProductId,\nreadonly name: string,\nreadonly description: string,\nreadonly category: string,\nreadonly variants: readonly ProductVariant[],\nreadonly imageUrls: readonly string[],\nreadonly reviewSummary: ProductReviewSummary,\nreadonly isActive: boolean,\n) {} get lowestPrice(): Money {\nconst prices = this.variants.map(v => v.price.amountInMinorUnits);\nconst lowest = Math.min(...prices);\nconst variant = this.variants.find(v => v.price.amountInMinorUnits === lowest)!;\nreturn variant.price;\n} get hasMultipleVariants(): boolean {\nreturn this.variants.length > 1;\n} get isAvailable(): boolean {\nreturn this.isActive && this.variants.some(v => v.stockCount > 0);\n} findVariant(variantId: string): ProductVariant | undefined {\nreturn this.variants.find(v => v.variantId === variantId);\n}\n}Cart Context — CartProduct (a reference, not a catalog)\ntypescript\n// cart/domain/cart-product-ref.value-object.ts\n// In the Cart context, \"Product\" is just a reference snapshot.\n// We don't need the full catalog data — just what we need to display the cart.\nimport { ProductId, Money } from '@shopflow/shared-kernel';export class CartProductRef {\nconstructor(\nreadonly productId: ProductId,\nreadonly variantId: string,\nreadonly name: string, // Snapshot at time of adding\nreadonly variantDescription: string, // e.g., \"Blue / Large\"\nreadonly unitPrice: Money, // Snapshot at time of adding\nreadonly imageUrl: string,\n) {} // No catalog methods here — no isAvailable, no variants list\n// This context only cares about what was added and at what price\n}Orders Context — OrderLineItem (a historical record)\ntypescript\n// orders/domain/order-line-item.entity.ts\nimport { ProductId, Money } from '@shopflow/shared-kernel';export class OrderLineItem {\nconstructor(\nreadonly lineItemId: string,\nreadonly productId: ProductId, // For linking back to catalog\nreadonly productName: string, // Frozen at time of purchase\nreadonly variantDescription: string,\nreadonly sku: string,\nreadonly unitPricePaid: Money, // Never changes\nreadonly quantity: number,\nreadonly totalPricePaid: Money,\n) {} // Order line items are immutable — no mutation methods\n// Business logic here is about the historical record\nget formattedTotal(): string {\nreturn this.totalPricePaid.format();\n}\n}This is not duplication — this is appropriate isolation. Each context owns exactly the data it needs and no more. If the Catalog context adds a new field (sustainabilityRating), the Cart and Orders contexts are not affected. If the Orders context needs to add returnEligible, it doesn't affect the catalog.\n\nAngular Module Architecture for Bounded Contexts\nTraditional NgModule Structure\nsrc/app/\n├── catalog/\n│ ├── domain/\n│ │ ├── catalog-product.entity.ts\n│ │ ├── category.entity.ts\n│ │ └── product.repository.ts ← interface only\n│ ├── application/\n│ │ └── catalog.facade.ts\n│ ├── infrastructure/\n│ │ ├── product-api.repository.ts ← HTTP implementation\n│ │ └── product.mapper.ts\n│ ├── presentation/\n│ │ ├── product-card/\n│ │ ├── product-list/\n│ │ └── product-detail/\n│ └── catalog.module.ts\n│\n├── cart/\n│ ├── domain/\n│ │ ├── cart.aggregate.ts\n│ │ ├── cart-item.entity.ts\n│ │ ├── cart-product-ref.value-object.ts\n│ │ └── cart.repository.ts\n│ ├── application/\n│ │ └── cart.facade.ts\n│ ├── infrastructure/\n│ │ ├── cart-api.repository.ts\n│ │ └── cart.mapper.ts\n│ ├── presentation/\n│ │ ├── cart-drawer/\n│ │ ├── cart-item/\n│ │ └── cart-summary/\n│ └── cart.module.ts\n│\n├── checkout/\n│ └── ...\n│\n├── orders/\n│ └── ...\n│\n└── shared-kernel/\n├── money.value-object.ts\n├── product-id.value-object.ts\n├── customer-id.value-object.ts\n├── address.value-object.ts\n└── shared-kernel.module.tsDefining the Module Boundaries\nEach bounded context module exposes only what other contexts are allowed to use — its public API:\ntypescript\n// catalog/catalog.module.ts\n@NgModule({\ndeclarations: [\nProductCardComponent,\nProductListComponent,\nProductDetailComponent,\n],\nimports: [\nCommonModule,\nSharedKernelModule,\nRouterModule.forChild(catalogRoutes),\n],\nexports: [\n// ✅ These are the public-facing components of the Catalog context\nProductCardComponent,\nProductListComponent,\n// ❌ CatalogFacade is NOT exported from the module — use DI tokens\n// ❌ Domain classes are NOT exported — they're implementation details\n],\nproviders: [\nCatalogFacade,\n{ provide: PRODUCT_REPOSITORY, useClass: ProductApiRepository },\n]\n})\nexport class CatalogModule {}The CatalogFacade is injectable across the app via DI — but the domain classes (CatalogProduct, ProductVariant) should ideally not be imported directly by other contexts. Other contexts should use the IDs and communicate through events or the shared kernel.\nBarrel Files as the Public API Contract\ntypescript\n// catalog/index.ts ← The ONLY file other contexts should import from\nexport { CatalogModule } from './catalog.module';\nexport { CatalogFacade } from './application/catalog.facade';\nexport { ProductCardComponent } from './presentation/product-card/product-card.component';\n// ❌ Do NOT export domain classes intended to be internal:\n// export { CatalogProduct } from './domain/catalog-product.entity'; // internalIf another developer tries to reach into catalog/domain/catalog-product.entity.ts directly, that's a boundary violation. The barrel file is the gatekeeper.\nEnforcing Boundaries with ESLint\nFor standard Angular CLI projects without Nx, you can use eslint-plugin-boundaries:\nbash\nnpm install --save-dev eslint-plugin-boundariesjavascript\n// .eslintrc.js\nmodule.exports = {\nplugins: ['boundaries'],\nsettings: {\n'boundaries/elements': [\n{ type: 'shared-kernel', pattern: 'src/app/shared-kernel/*' },\n{ type: 'catalog', pattern: 'src/app/catalog/*' },\n{ type: 'cart', pattern: 'src/app/cart/*' },\n{ type: 'checkout', pattern: 'src/app/checkout/*' },\n{ type: 'orders', pattern: 'src/app/orders/*' },\n]\n},\nrules: {\n'boundaries/element-types': ['error', {\ndefault: 'disallow',\nrules: [\n// Any context can import from shared-kernel\n{ from: '*', allow: ['shared-kernel'] },\n// Cart can reference Catalog (to show product names in cart)\n// but only through the Catalog's public facade, not domain internals\n{ from: 'cart', allow: ['catalog'] },\n// Checkout can reference Cart\n{ from: 'checkout', allow: ['cart', 'catalog'] },\n// Orders is standalone — references shared-kernel only\n{ from: 'orders', allow: ['shared-kernel'] },\n]\n}]\n}\n};For Nx projects, use @nx/enforce-module-boundaries in project tags — it's even more powerful.\n\nCross-Context Communication: How Contexts Talk to Each Other\nBounded contexts should not directly import each other’s domain models. But they still need to communicate. Here are the three patterns for ShopFlow:\nPattern 1: Shared IDs (The Most Common)\nThe Cart context needs to display a product name. It has a ProductId from the CartProductRef. To get the full details, it uses the CatalogFacade:\ntypescript\n// cart/application/cart.facade.ts\n@Injectable({ providedIn: 'root' })\nexport class CartFacade {\nconstructor(\nprivate store: Store,\nprivate catalogFacade: CatalogFacade, // ✅ Facade-to-facade, not domain-to-domain\n) {} // When displaying cart items, enrich them with catalog data if needed\ngetCartItemsWithDetails(): Observable<CartItemViewModel[]> {\nreturn this.store.select(selectCartItems).pipe(\nswitchMap(cartItems => {\nconst productIds = cartItems.map(item => item.productRef.productId);\nreturn this.catalogFacade.getProductsByIds(productIds).pipe(\nmap(products => cartItems.map(item => ({\n...item,\ncurrentCatalogPrice: products\n.find(p => p.id.equals(item.productRef.productId))\n?.lowestPrice\n})))\n);\n})\n);\n}\n}Pattern 2: Domain Events (Covered in Article 7)\nWhen checkout completes, the Cart context publishes a CartCheckedOut event. The Orders context subscribes to this event and creates an order. Neither context imports the other's domain model.\nPattern 3: Shared Kernel Types\nBoth Cart and Catalog use ProductId from the shared kernel. The ID is the bridge — the Cart doesn't need to"}