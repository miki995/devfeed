{"id":"aef4dbd943b7","content":"Angular\n\nDdd\n\n8 min read\n4 hours ago\n\n--\n\nPress enter or click to view image in full size\n\nThe difference between a Shared Kernel and a SharedModule is discipline. Here’s how to maintain it.\n\nIn the Bounded Contexts article, we introduced the Shared Kernel as a small, curated set of concepts shared identically across all contexts. We said it wasn’t a dumping ground.\nAfter a few months on a real project, that principle gets tested.\nA developer needs Money in checkout — so it goes to shared-kernel. Someone needs a product status pipe, and it's faster to put it in shared than figure out where it belongs. Then a modal component. Then a UserService. Then a CartHelper that \"everyone needs.\"\nSound familiar? You’ve just rebuilt the SharedModule problem with a new name.\nThe Shared Kernel has to be actively defended. This article gives you the rules, the tests, and the tooling to keep it from growing into the thing it was meant to replace.\n\nThe Four Tests\nA concept earns its place in the Shared Kernel only if it passes all four:\nTest 1: Identical semantics everywhere. Money in Cart means exactly the same thing as Money in Checkout and Orders. No context-specific behavior. Compare with Product — which means something different in every context and rightly lives in each one separately.\nTest 2: No context-specific extensions. If any context needs to subclass or extend it for local behavior, it doesn’t belong here. A Shared Kernel type is used as-is, or not at all.\nTest 3: Changing it requires coordinating all contexts. This is the DDD definition. The Shared Kernel is a deliberate coupling — justified only because the concept is genuinely universal. If a change only affects one context, it belongs in that context.\nTest 4: Zero dependencies on any bounded context. The Shared Kernel imports nothing from Catalog, Cart, Checkout, or any other context. It is depended upon — it never depends.\n\nWhat Actually Belongs There\nAfter running ShopFlow’s shared concepts through the four tests:\nshared-kernel/\n├── value-objects/\n│ ├── money.value-object.ts ✅ Identical meaning everywhere\n│ ├── email.value-object.ts ✅ Used in Profile, Auth, Checkout\n│ ├── address.value-object.ts ✅ Checkout, Orders, Profile — same meaning\n│ ├── date-range.value-object.ts ✅ Coupons, promotions, shipment windows\n│ └── ids/\n│ ├── typed-id.base.ts\n│ ├── product-id.ts\n│ ├── cart-id.ts\n│ ├── order-id.ts\n│ ├── customer-id.ts\n│ └── cart-item-id.ts\n├── errors/\n│ ├── domain-error.ts ✅ Base class — all contexts use it\n│ ├── application-error.ts ✅ Base class\n│ └── http-error.mapper.ts ✅ Shared translation logic\n├── result.ts ✅ Used by all domain operations\n├── entity.base.ts ✅ Extended by all entities\n├── domain-event-bus.ts ✅ Cross-context infrastructure\n└── index.ts ← Public APIAnd what does not belong:\nshared-kernel/\n├── product-card.component.ts ❌ Catalog context\n├── order-summary.component.ts ❌ Orders context\n├── cart.service.ts ❌ Cart context\n├── product.model.ts ❌ Catalog context\n├── order-status.pipe.ts ❌ Orders context\n└── format-price.ts ❌ Should be Money.format()That last entry is worth calling out. If format-price.ts exists because Money.format() didn't, the right fix is to add Money.format() — not to add a utility function to the shared folder.\n\nThe ID System: The Legitimate Cross-Context Bridge\nIDs are the most naturally shared concept in ShopFlow. The Catalog creates ProductId. The Cart stores ProductId references. Orders stores ProductId references. The ID travels across contexts — the entity it identifies stays in its own context.\n// shared-kernel/value-objects/ids/typed-id.base.ts\nexport abstract class TypedId {\nreadonly #value: string; // True private field\nprotected constructor(value: string) {\nconst trimmed = value?.trim();\nif (!trimmed || trimmed.length === 0) {\nthrow new Error(`${this.constructor.name} cannot be empty`);\n}\nif (trimmed.length > 128) {\nthrow new Error(`${this.constructor.name} exceeds 128 character maximum`);\n}\nthis.#value = trimmed;\n}\nget value(): string { return this.#value; }\nequals(other: this): boolean { return this.#value === other.#value; }\ntoString(): string { return this.#value; }\ntoJSON(): string { return this.#value; }\n}// Each domain concept gets its own ID class with a factory method\nexport class ProductId extends TypedId {\nstatic generate(): ProductId { return new ProductId(crypto.randomUUID()); }\nstatic from(v: string): ProductId { return new ProductId(v); }\n}export class CartId extends TypedId { static generate() { return new CartId(crypto.randomUUID()); } }\nexport class OrderId extends TypedId { static generate() { return new OrderId(crypto.randomUUID()); } }\nexport class CustomerId extends TypedId { static generate() { return new CustomerId(crypto.randomUUID()); } }\nexport class CartItemId extends TypedId { static generate() { return new CartItemId(crypto.randomUUID()); } }\n\nAddress — A Multi-Context Value Object\nAddress appears in three ShopFlow contexts with the same meaning in all of them: a physical location with street, city, postcode, and country. It passes all four tests — which is why it belongs in the Shared Kernel.\n// shared-kernel/value-objects/address.value-object.ts\nexport type CountryCode = 'DE' | 'AT' | 'CH' | 'FR' | 'GB' | 'US' | 'NL' | 'BE';\nconst COUNTRY_NAMES: Record<CountryCode, string> = {\nDE: 'Germany', AT: 'Austria', CH: 'Switzerland',\nFR: 'France', GB: 'United Kingdom', US: 'United States',\nNL: 'Netherlands', BE: 'Belgium',\n};\nexport class Address {\nreadonly line1: string;\nreadonly line2: string | undefined;\nreadonly city: string;\nreadonly postcode: string;\nreadonly countryCode: CountryCode;\nreadonly state: string | undefined;\nconstructor(props: {\nline1: string; line2?: string; city: string;\npostcode: string; countryCode: CountryCode; state?: string;\n}) {\nif (!props.line1?.trim()) throw new Error('Address line 1 is required');\nif (!props.city?.trim()) throw new Error('City is required');\nif (!props.postcode?.trim()) throw new Error('Postcode is required');\nif (!COUNTRY_NAMES[props.countryCode]) {\nthrow new Error(`Unsupported country: ${props.countryCode}`);\n}\nif (props.countryCode === 'US' && !props.state?.trim()) {\nthrow new Error('State is required for US addresses');\n}\nthis.line1 = props.line1.trim();\nthis.line2 = props.line2?.trim() || undefined;\nthis.city = props.city.trim();\nthis.postcode = this.normalizePostcode(props.postcode, props.countryCode);\nthis.countryCode = props.countryCode;\nthis.state = props.state?.trim() || undefined;\n}\nget countryName(): string { return COUNTRY_NAMES[this.countryCode]; }\nformat(style: 'inline' | 'multiline' = 'multiline'): string {\nconst parts = [\nthis.line1, this.line2, this.city,\nthis.state, this.postcode, this.countryName\n].filter(Boolean) as string[];\nreturn style === 'inline' ? parts.join(', ') : parts.join('\\n');\n}\n// Immutable update - returns a new Address\nwith(changes: Partial<ConstructorParameters<typeof Address>[0]>): Address {\nreturn new Address({\nline1: this.line1, line2: this.line2, city: this.city,\npostcode: this.postcode, countryCode: this.countryCode,\nstate: this.state, ...changes\n});\n}\nequals(other: Address): boolean {\nreturn this.line1 === other.line1 && this.line2 === other.line2 &&\nthis.city === other.city && this.postcode === other.postcode &&\nthis.countryCode === other.countryCode && this.state === other.state;\n}\nprivate normalizePostcode(postcode: string, country: CountryCode): string {\nconst cleaned = postcode.trim().toUpperCase().replace(/\\s+/g, ' ');\n// UK postcodes need a space: \"SW1A1AA\" → \"SW1A 1AA\"\nif (country === 'GB') {\nreturn cleaned.replace(/^([A-Z]{1,2}\\d{1,2}[A-Z]?)(\\d[A-Z]{2})$/, '$1 $2');\n}\nreturn cleaned;\n}\n}\nDateRange — Another Legitimate Shared Concept\nCoupons have validity periods. Promotions have start and end dates. Shipment windows are date ranges. Same concept, same meaning, used across multiple contexts.\n// shared-kernel/value-objects/date-range.value-object.ts\nexport class DateRange {\nreadonly start: Date;\nreadonly end: Date;\nconstructor(start: Date, end: Date) {\nif (start >= end) {\nthrow new Error(\n`Start must be before end: ${start.toISOString()} – ${end.toISOString()}`\n);\n}\nthis.start = new Date(start);\nthis.end = new Date(end);\n}\nget durationInDays(): number {\nreturn Math.ceil((this.end.getTime() - this.start.getTime()) / 86_400_000);\n}\ncontains(date: Date): boolean { return date >= this.start && date <= this.end; }\nget isActive(): boolean { return this.contains(new Date()); }\nget isExpired(): boolean { return new Date() > this.end; }\nget isUpcoming(): boolean { return new Date() < this.start; }\noverlaps(other: DateRange): boolean {\nreturn this.start < other.end && this.end > other.start;\n}\nequals(other: DateRange): boolean {\nreturn this.start.getTime() === other.start.getTime() &&\nthis.end.getTime() === other.end.getTime();\n}\n}\nThe Public API: index.ts\nThe barrel file is the contract. Only what appears here is accessible to other contexts:\n// shared-kernel/index.ts\n// Value Objects\nexport { Money } from './value-objects/money.value-object';\nexport type { CurrencyCode } from './value-objects/money.value-object';\nexport { Email } from './value-objects/email.value-object';\nexport { Address } from './value-objects/address.value-object';\nexport type { CountryCode } from './value-objects/address.value-object';\nexport { DateRange } from './value-objects/date-range.value-object';\n// IDs\nexport { ProductId, CartId, CartItemId, OrderId, CustomerId }\nfrom './value-objects/ids';\n// Base classes\nexport { Entity } from './entity.base';\nexport { TypedId } from './value-objects/ids/typed-id.base';\n// Error hierarchy\nexport { DomainError } from './errors/domain-error';\nexport { ApplicationError } from './errors/application-error';\nexport { HttpErrorMapper } from './errors/http-error.mapper';\n// Result type\nexport { Result } from './result';\n// Infrastructure\nexport { DomainEventBus } from './domain-event-bus';\nexport type { DomainEvent } from './domain-event-bus';Anything not listed here doesn’t exist as far as the rest of the application is concerned.\n\nDomain Kernel vs. UI Library — Keep Them Separate\nThe Shared Kernel contains two distinct categories that often get conflated:\nDomain Shared Kernel — framework-agnostic domain concepts. Money, Address, ProductId, DomainError. These could run in a Node.js service or a React app. No Angular dependency required.\nUI Component Library — generic Angular presentational components. ButtonComponent, ModalComponent, MoneyFormatPipe. No domain knowledge. Pure display.\nKeep them in separate directories — ideally separate Nx libraries:\nlibs/\nshared-kernel/ ← Domain concepts only\nvalue-objects/\nerrors/\nentity.base.ts\nresult.ts\ndesign-system/ ← UI primitives, no domain knowledge\ncomponents/\nbutton/\ninput/\nmodal/\npipes/\nmoney-format.pipe.tsThe confusion between these two categories is usually how product-card.component.ts ends up in the shared kernel. A product card knows about products — it belongs in the Catalog context. A button knows about nothing — it belongs in the design system.\n\nGovernance: Keeping the Kernel Small\nTwo practical rules that prevent the Shared Kernel from becoming the next SharedModule:\nThe Three Contexts Rule — a concept moves to the Shared Kernel only when at least three different bounded contexts need it with identical semantics. Before that threshold, keep it in the first context that needs it and export it explicitly from that context’s barrel file.\nThe No Extensions Rule — if any context needs to extend or specialize a Shared Kernel type for local purposes, it doesn’t belong in the Shared Kernel. Context-specific needs stay in their context.\nAutomated Enforcement\n// .eslintrc.js\n'boundaries/element-types': ['error', {\ndefault: 'disallow',\nrules: [\n// Shared Kernel imports from nothing — ever\n{ from: 'shared-kernel', allow: ['shared-kernel'] },\n// All contexts can import from Shared Kernel\n{ from: ['catalog', 'cart', 'checkout', 'orders', 'profile'],\nallow: ['shared-kernel'] },\n]\n}]In Nx, contexts import from the libra"}