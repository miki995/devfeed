{"id":"2a2d4e9e70c6","content":"4 min read\n1 hour ago\n\n--\n\nEvery Angular developer has stared at a sprawling NgRx codebase and thought, “Do I really need an Action, Reducer, Effect, and Selector just to toggle a dropdown or manage a multi-step form?”\nFor years, we accepted the boilerplate because the predictable, event-driven architecture of NgRx kept our enterprise applications from collapsing into chaos. But when the Angular team introduced Signals, the community started asking a dangerous question: Should we abandon traditional NgRx entirely?\nThe answer is a resounding no.\nThe new gold standard in modern Angular architecture isn’t about choosing between global event-driven state and local reactive state. It’s about seamlessly combining them. Here is how to build a scalable, developer-friendly architecture using traditional NgRx for global orchestration and the new NgRx SignalStore for highly reactive, local feature state.\nThe Core Philosophy: Two Brains are Better Than One\nTo understand this architecture, you have to stop looking at state as a single monolith. Your application actually has two different “nervous systems,” and they need different tools.\n\n• The Global Nervous System (Traditional NgRx)\nThis is your app’s central hub. It is event-driven, declarative, and shared. Components here don’t mutate state; they shout into the void that something happened (e.g., ⁠[Auth] User Logged In⁠), and the global store decides how to react.\nWhat lives here?\nUser authentication and permissions.\nApplication-wide settings (dark mode, language preferences).\nShared entity caches used across completely unrelated features.\nCross-cutting events (handling 401 Unauthorized API responses via Effects).\n2. The Local Brain (NgRx SignalStore)\nThis is the state tied directly to a specific feature or route. It is method-based, imperative, and highly reactive. It lives and dies with the component or route it serves.\nWhat lives here?\nThe data currently loaded in a specific data grid or dashboard.\nPagination, sorting, and filtering parameters for a single page.\nComplex, multi-step form state.\nDirect API orchestrations for a localized feature.\nThe Blueprint: Folder Structure\nWhen you mix paradigms, your boundaries must be visually obvious the second a developer opens the repository. We lean heavily into Domain-Driven Design (DDD) to keep the global state pure and the feature states isolated.\nsrc/app/\n│\n├── core/ # 🌍 THE GLOBAL ZONE (Loaded on startup)\n│ ├── services/\n│ └── state/ # Traditional NgRx\n│ ├── app.state.ts\n│ ├── auth/\n│ │ ├── auth.actions.ts\n│ │ ├── auth.effects.ts\n│ │ └── auth.reducer.ts\n│\n├── shared/ # 🧱 REUSABLE UI (Dumb components only)\n│ └── components/\n│ └── ui-button/ # No state injected here!\n│\n└── features/ # 🧩 THE FEATURE ZONE (Lazy-loaded)\n│\n└── profile/\n├── components/\n├── services/\n├── profile.routes.ts\n└── store/ # Local SignalStore\n└── profile.store.ts\nThe Golden Rule: Features are completely selfish. The ⁠profile⁠ feature is never allowed to import a SignalStore from the ⁠orders⁠ feature. If they need to communicate, the feature must dispatch a global action.\nBuilding the Bridge: How They Talk\nThe true power of this architecture is how easily these two systems interact. Because both are built for the modern Angular ecosystem, your local SignalStore can inject the global ⁠Store⁠ to read global context and dispatch global events.\nLet’s look at a practical example. Imagine a ⁠ProfileStore⁠ (our local SignalStore) that needs to update a user’s avatar. It needs the ⁠userId⁠ from the Global Store to make the API call, and once successful, it needs to dispatch a global action so the app’s top navigation bar updates the user’s picture.\nimport { inject } from '@angular/core';\nimport { Store } from '@ngrx/store';\nimport { signalStore, withState, withMethods, patchState, withComputed } from '@ngrx/signals';\nimport { rxMethod } from '@ngrx/signals/rxjs-interop';\nimport { pipe, switchMap, tap } from 'rxjs';// 1. Import Global State selectors and actions\nimport { selectUserId } from '../../core/state/auth/auth.selectors';\nimport { AuthActions } from '../../core/state/auth/auth.actions';\nimport { ProfileApiService } from '../services/profile-api.service';\ninterface ProfileState {\nisUploading: boolean;\nuploadError: string | null;\n}\nconst initialState: ProfileState = {\nisUploading: false,\nuploadError: null,\n};\nexport const ProfileStore = signalStore(\nwithState(initialState),\n// 2. Read from the Global Store using computed signals\nwithComputed((_, globalStore = inject(Store)) => ({\ncurrentUserId: globalStore.selectSignal(selectUserId)\n})),\nwithMethods((\nstore,\napi = inject(ProfileApiService),\nglobalStore = inject(Store) // Inject the global NgRx Store\n) => ({\n// 3. Imperative method called directly by the UI component\nupdateAvatar: rxMethod<File>(\npipe(\ntap(() => patchState(store, { isUploading: true, uploadError: null })),\nswitchMap((file) => {\n// Use the computed signal derived from global state\nconst userId = store.currentUserId();\nreturn api.uploadAvatar(userId, file).pipe(\ntap({\nnext: (response) => {\npatchState(store, { isUploading: false });\n// 4. Dispatch to the Global Store so the rest of the app reacts!\nglobalStore.dispatch(AuthActions.avatarUpdated({ newUrl: response.url }));\n},\nerror: (err) => {\npatchState(store, { isUploading: false, uploadError: err.message });\n}\n})\n);\n})\n)\n)\n}))\n);\nWhy this is a game-changer:\n\n• Zero Global Clutter: The global store doesn’t need to know about ⁠isUploading⁠ or ⁠uploadError⁠. That transient UI state is handled perfectly by the local SignalStore.\n\n• 2. Strict Traceability: The moment the avatar actually changes, a formal action (⁠[Auth] Avatar Updated⁠) is dispatched globally, allowing the Redux DevTools to track the data flow across the app.\n\n• 3. Incredible DX: The component developer just calls ⁠profileStore.updateAvatar(file)⁠. They don’t have to wire up complex effects or track loading selectors.\n\n• The Takeaway\n\n• By marrying traditional NgRx with the new SignalStore, you get the best of both worlds. You retain the strict, event-driven traceability required to keep an enterprise app stable, while recovering the rapid, imperative developer experience that makes building individual features actually fun again.\n\n• Stop fighting the boilerplate, and start putting your state where it actually belongs."}